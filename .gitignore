# macOS / Xcode
.DS_Store
xcuserdata/
*.xcuserstate
build/
DerivedData/

# Generated by XcodeGen in CI — never commit the project, only project.yml
*.xcodeproj/

# Swift Package Manager (local/Linux engine verification only — not part of the app build)
.build/
Package.resolved

# Secrets — never commit
*.env
.secrets/
AuthKey_*.p8
